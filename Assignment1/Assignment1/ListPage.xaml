<?xml version="1.0" encoding="UTF-8"?>
<views:MvxContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:Asssignment1" 
             xmlns:views="clr-namespace:MvvmCross.Forms.Views;assembly=MvvmCross.Forms"
             x:Class="Assignment1.ListPage">
    <ContentPage.Content>
        <ListView x:Name="listView" SeparatorColor="White" SeparatorVisibility="Default" RowHeight="55"
                  BackgroundColor="Teal" ItemsSource="{Binding Pastimes}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Vertical">
                            <Label Text="{Binding Name}" TextColor="White" HorizontalOptions="StartAndExpand" FontSize="18" VerticalOptions="End" HeightRequest="25" />
                            <Label Text="{Binding Description}" TextColor="Silver" HorizontalOptions="StartAndExpand" FontSize="16" VerticalOptions="Start" HeightRequest="25" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</views:MvxContentPage>
